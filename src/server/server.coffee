fs = require "fs"
async = require "async"
hat = require "hat"
_ = require "lodash"
liburl = require "url"
libpath = require "path"

module.exports = class Server
  constructor: ->

  init: ->

  listen: (port, f) ->

  stats: ->
    { running: true }
